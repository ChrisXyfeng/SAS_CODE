/*计算一个表格中行数*/
PROC SQL;
	SELECT COUNT(*) AS N 
	FROM (SELECT DISTINCT
	   t1.NIP,
	   t2.DATE_KT,
	   t2.PDS,
	   t2.TAILLE
	FROM WORK.NIP t1 LEFT JOIN WORK.CARDIO t2 ON t1.NOM = t2.NOM AND t1.PRENOM = t2.PRENOM AND t1.DN = t2.DN
	WHERE TAILLE IS NOT NULL AND PDS IS NOT NULL);
QUIT;
